# ores-otel-log-rust-test

Exact-head **rust** conformance harness for `ores-otel/ores.otel.log` and `ORESoftware/next-loggers.ts`.

Native verification command: `cargo test --all-features && cargo clippy --all-targets --all-features -- -D warnings`.

Promotion requires both sources to pass at explicit 40-character commit SHAs.
